#include <stdio.h>

int main()
{
  int height = 15;
  int width = 12;
  int area;
  
  area = height * width;
  printf("The height is %d, the width is %d.\n", height, width);
  printf("And the area is : %d \n", area);
  
  return 0;
}	
